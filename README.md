# my-portfolio-chatbot
1. vs code
2. python install
3. pip install flask
6. pip install flask-cors
4. pip install numpy 
5. pip install scikit-learn
7. pip install flask scikit-learn nltk numpy pandas flask-cors
8. python -c "import nltk; nltk.download('punkt_tab')"
